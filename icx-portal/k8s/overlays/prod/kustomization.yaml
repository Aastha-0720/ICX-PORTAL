apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base                       # shared base (deployments, services, statefulsets)
  - secret-provider-class.yaml       # prod: vaultKubernetesMountPath=kubernetes
  - ghcr-secret-provider-class.yaml  # prod: vaultKubernetesMountPath=kubernetes
